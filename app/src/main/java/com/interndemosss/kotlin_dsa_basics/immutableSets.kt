package com.interndemosss.kotlin_dsa_basics

fun main(){

        //  Set : It  is a Collection of unOrdered Unique elements
        //     ( No Duplicates Elements )
        //Immutable Sets
        val colors = setOf("Red","White","Orange")

        //iterating over the  set elements
        for (item in colors){
            println(item)
        }

}